{
  "name": "satellite-tracker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Real-time satellite tracker using free CelesTrak data and satellite.js",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm --workspace client run dev",
    "build": "npm --workspace client run build",
    "preview": "npm --workspace client run preview",
    "dev:server": "npm --workspace server run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
